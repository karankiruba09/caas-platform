# Disruption budget: keep the frontend available during node drains / rollouts.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: productpage
  namespace: tenant-bookinfo
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: productpage
