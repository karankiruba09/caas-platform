# ⚠️  DEMO CREDENTIALS ONLY — committed for a self-contained POC.
# In production, never commit secrets: use Sealed Secrets, External Secrets
# Operator, or Vault. These passwords are intentionally throwaway.
apiVersion: v1
kind: Secret
metadata:
  name: wordpress-db
  namespace: tenant-wordpress
type: Opaque
stringData:
  mysql-root-password: "demo-root-pw-change-me"
  mysql-database: "wordpress"
  mysql-user: "wordpress"
  mysql-password: "demo-wp-pw-change-me"
  wp-admin-user: "admin"
  wp-admin-password: "demo-admin-pw-change-me"
