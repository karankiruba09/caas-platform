# DEMO CREDENTIALS ONLY — committed so the POC is self-contained.
# Do not reuse these values outside the demo. Production must use Sealed Secrets,
# External Secrets Operator, Vault, or an equivalent secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: wordpress-db
  namespace: tenant-wordpress
type: Opaque
stringData:
  mysql-root-password: "demo-root-pw-change-me"
  mysql-database: "wordpress"
  mysql-user: "wordpress"
  mysql-password: "demo-wp-pw-change-me"
  wp-admin-user: "admin"
  wp-admin-password: "demo-admin-pw-change-me"
